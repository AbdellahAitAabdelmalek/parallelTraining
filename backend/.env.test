# Base de données locale (même DB que le dev, ou une DB de test dédiée)
DATABASE_URL=postgresql://parallel_user:parallel_password@localhost:5433/parallel_db_test

# Valeurs factices — les guards JWT sont overridés en test, ces valeurs ne sont jamais utilisées
SUPABASE_URL=http://localhost:54321
SUPABASE_JWT_SECRET=test-secret-placeholder

# Valeur factice — le client OpenAI est initialisé mais jamais appelé (services mockés)
OPENAI_API_KEY=sk-test-00000000000000000000000000000000000000000000000000
